name: test

on:
  push:
    branches: [ "master" ]

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Invoke deployment hook
        uses: distributhor/workflow-webhook@v3
        with:
          webhook_url: http://82.97.240.173:8080/github-webhook/
          verbose: true
          webhook_type: 'json-extended'


        
